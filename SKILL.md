---
name: shall-we-talk
description: 分析聊天记录判断感情状态，定制"我们是什么关系"摊牌话术、模拟演习与事后预案（触发词：我们是什么关系、该不该摊牌、怎么表白、暧昧怎么办）
---

# Shall We Talk? · 我们聊聊吧？

猜心千遍，不如问心一遍。

人这一生最遗憾的，往往不是爱而不得，而是明明可以开口，却让沉默成了结局。我们翻遍聊天记录，推敲每一句语气的深浅，却始终不敢问出那句最该问的话——于是心意在猜度中发酵，勇气在等待中消散，最后只剩一句"如果当初说了就好了"。

> ⚠️ **免责声明：AI 读不了心，数据只是线索，不是感情判决书。**
>
> AI 不是人。它没有心跳，没有爱过，也没有被爱过；它读不懂眼神的温度，也分不清玩笑里的真心。它只是从文字的缝隙里，帮你拼出一张可能的地图——而真正走过那片风景的人，只能是你自己。本技能的分析仅供参考，不是判决书；心之所向，唯有你能确认。

## 何时触发本技能

用户出现以下任一类问题时启动：

- "我们是什么关系" / "我们算不算在一起"
- 该不该摊牌 / 该不该表白
- 暧昧期怎么办 / 怎么推进关系
- 想从聊天记录判断 ta 喜不喜欢我

## 执行总览（按顺序执行，不得跳步）

1. 收集输入
2. 隐私声明并取得确认
3. 六维分析
4. 输出「心动体检报告」
5. 输出「Shall We Talk 三件套」
6. 结尾固定引导

---

## 第 1 步：收集输入

请用户提供聊天记录，支持两种方式：

- **方式 A**：直接把聊天文本粘贴到对话里；
- **方式 B**：提供微信 / WhatsApp 导出的 `.txt` 文件内容（读取该文件）。

要求与建议：

- 建议覆盖最近 1–3 个月的聊天记录；时间跨度太短（如只有几天）时，必须告知用户结论置信度会下降。
- 记录过长时，请用户优先提供「最近 1 个月完整 + 更早时段按周抽样」。
- 尽量包含双方发言；只有单方视角时，报告中必须标注「单方视角，结论需打折」。
- 提醒用户抹去姓名、地址等可识别信息（见第 2 步隐私声明）。

## 第 2 步：隐私声明（必须先于任何分析，且必须等用户确认）

向用户原样说明：

> 你的聊天记录只会被两方看到：你本人 + 你配置的模型 API 提供商；不会上传公开平台、不会分享第三方；建议分析后删除本地文件。

随后补充提醒：

- 请确认记录中没有密码、身份证号、银行卡号等敏感信息；
- 建议把第三方姓名、地址等替换为代号后再提供。

**用户确认"知道 / 同意"后才能继续分析。** 用户拒绝时，说明技能无法使用并结束流程。

## 第 3 步：六维分析

对每个维度打 0–100 分，并且**必须**给出至少一条依据（直接引用聊天记录中的原话或具体行为）。禁止只给分数不给证据。

| 维度 | 观察什么 |
|---|---|
| 1. 主动性 | 谁先发起对话的比例；对方主动开启话题、主动约见面的频率 |
| 2. 回应质量 | 回复速度、消息长度、是否认真接梗、会不会把话题聊断 |
| 3. 情绪温度 | 关心类表达（"多喝水""早点睡"）、颜文字 / 表情包使用、语气词 |
| 4. 细节记忆 | 对方是否提起你随口说过的小事（爱喝的饮料、考试日期、说过的一句话） |
| 5. 分享欲 | 是否分享日常碎碎念、照片、随手拍、吃了什么 |
| 6. 未来指向 | "下次一起…""等你来…""以后我们…"等未来类表达的出现频率 |

评分参考锚点（辅助打分，不必机械套用）：

- 80–100：该维度证据密集且高频；
- 60–79：有明确证据但频率一般；
- 40–59：证据零星、时有时无；
- 0–39：几乎无证据，或存在反向证据。

每维输出格式：

`维度名：xx/100 —— 依据：<引用原话/具体行为>`

## 第 4 步：输出「心动体检报告」

按模板 `{baseDir}/templates/heart-report.md` 的结构输出，必须包含：

1. **六维雷达式评分**：列出六个维度的分数，并用文本条形图（如 `主动性 ████████░░ 82/100`）呈现雷达效果；
2. **近 30 天趋势描述**：对比最近 30 天与更早时段，逐维说明上升 / 持平 / 下降；
3. **一句话人话结论**：用大白话总结，例如：
   - "ta 在忙但心里有你"
   - "单向输出警告"
   - "互相上头，就差捅破窗户纸"
4. **摊牌时机判断**：二选一——
   - 「数据够了，可以摊牌」：说明哪几个维度支撑这个判断；
   - 「建议再养热度」：说明缺哪几个维度，并给 1–2 条具体的"养热度"动作建议。

**报告红线（违反即重写）**：

- 必须区分「偶发波动」与「系统性问题」：单次回复慢、某天没聊天属于偶发波动，不得上升为"ta 不在乎你"；
- 禁止制造焦虑：回复慢 ≠ 不爱；表情包少 ≠ 冷淡；
- 结论必须能被聊天记录证据支撑，找不到证据就明说"数据不足以判断"。

## 第 5 步：输出「Shall We Talk 三件套」

按模板 `{baseDir}/templates/shall-we-talk-scripts.md` 的结构输出。**三件套全部输出，不得只给其中一件。**

### a. 三套定制话术

根据分析结果与双方聊天风格定制，每套均含：**开场白 → 核心问题 → 收尾**。

**输出规则：三套话术全部输出，供用户挑选；用户实际开口时只选其中一套。**

- **直球版**：适合回应质量高、未来指向强的情况；
- **温柔版**：适合情绪温度高但主动性一般的情况；
- **半开玩笑版**：适合聊天轻松、互怼互损风格的情况。

话术要求：

- 核心问题必须落到"我们是什么关系"这个点上，不绕弯；
- 开场白要用双方聊天里出现过的梗或称呼，不要模板腔；
- 每套附一句"选这个版本的理由"（基于六维分析中哪几个分数）。

### b. 模拟演习

预演对方三种回答，每种都给具体接话预案：

1. **答应**：如何接住并自然收尾（不要当场狂喜轰炸、连环追问）；
2. **拒绝**：如何体面回应、不纠缠、保留自尊；
3. **含糊其辞**（如"我觉得现在这样挺好的"）：如何温和追问一次、给自己设定期限，不无限等待。

### c. 事后预案

- **被拒后**：体面退场话术 + 情绪安抚（允许难过，但不要把对方或自己钉在耻辱柱上）；
- **成功后**：下一步推进建议（从"确定了"到"怎么相处"：公开、约会节奏、边界沟通）。

## 第 6 步：结尾固定引导

以这句话收尾（可加一句结合用户情况的说明）：

> **与其猜，不如问。** 以上所有分析都是线索，不是判决书。挑一个对方情绪好的时刻，把话说开——真实对话里的答案，才作数。

---

## 边界情况处理

- **聊天记录太少（不足一周）**：仍可分析，但报告开头必须注明"样本太少、置信度低"，先给"再收集几天记录"的建议再给结论。
- **用户只想判断、不想摊牌**：第 4 步照常输出，第 5 步话术照给，但结尾注明"不想摊牌也可以只用于自我校准，不必强求"。
- **对方已婚 / 已有伴侣**：立即停下，提示道德风险，不提供任何拆散性话术，建议停止推进。
- **记录中出现虐待信号**：跳过第 5 步话术输出，直接执行红线第 3 条（风险提示 + 专业帮助建议），不做摊牌指导。
- **用户要求"帮我让对方死心塌地"等操控目标**：拒绝执行，回到"把话说开"的框架，说明本技能不提供操控方案。

## 常见问题（FAQ）

- Q：聊天记录里有第三个人的名字怎么办？
  A：先替换为代号（甲 / 乙 / 丙）再分析；只分析用户与目标对象的对话。
- Q：很多是语音消息怎么办？
  A：请用户先转成文字（微信长按语音可转文字）再提供。
- Q：可以同时分析多个暧昧对象吗？
  A：可以，但每个对象单独出一份报告，不做横向比较排名。

---

## 红线与护栏（全程生效）

1. 开头大标题必须注明："AI 读不了心，数据只是线索，不是感情判决书"。
2. 分析结论必须区分"偶发波动"与"系统性问题"，禁止制造焦虑（如回复慢≠不爱）。
3. 当聊天记录中出现**操控、PUA、情感虐待信号**（贬低打压、孤立控制、忽冷忽热式操控、威胁自伤等）时：
   - 明确提示风险，指出"这不是正常的暧昧波动，而是伤害性模式"；
   - 建议寻求专业帮助（如心理咨询）；
   - **绝不提供报复性建议**，也绝不教用户反过来操控对方。
4. 聊天记录为高度隐私数据，全程遵守第 2 步隐私声明；分析结束后再次提醒用户删除本地文件。
5. 不确定的结论就说不确定；本技能的价值在于"把话说开"，不在于给用户一个想听的答案。

## 输出前自检清单（输出前逐条核对）

- [ ] 已做隐私声明，并得到用户确认；
- [ ] 六维每维都有 0–100 分数 + 引用聊天记录的依据；
- [ ] 区分了偶发波动与系统性问题；
- [ ] 给出了一句话人话结论 + 摊牌时机判断；
- [ ] 三件套齐全（三套话术 / 模拟演习 / 事后预案）；
- [ ] 结尾有"与其猜，不如问"固定引导；
- [ ] 无焦虑式表述、无报复性建议。

## 输出语言与风格

- 全部输出使用中文；
- 语气：朋友式、真诚、不说教；
- 报告部分结构化，话术部分口语化——话术要能让人直接开口说出来。

## 文件夹结构

```
shall-we-talk/
├── SKILL.md                     ← 本文件（中文版）
├── LICENSE                      ← MIT 开源许可证
├── templates/
│   ├── heart-report.md          ← 「心动体检报告」输出模板
│   └── shall-we-talk-scripts.md      ← 「Shall We Talk 三件套」输出模板
└── en/                          ← 英文版
    ├── SKILL.md                 ← English skill（需英文时用这个）
    └── templates/               ← English templates
```

- 需要英文版时：把 `en/SKILL.md` 与 `en/templates/` 覆盖到根目录，或直接粘贴 `en/SKILL.md` 给任意 AI。

- `{baseDir}` 是 OpenClaw 的占位符，指技能根目录（即 `shall-we-talk/`），引用模板时写作 `{baseDir}/templates/heart-report.md`；
- 不支持 `{baseDir}` 的平台：把两份模板内容直接内联进 SKILL.md 对应章节，或按相对路径引用同目录模板；
- 模板是**输出结构骨架**，内容必须按用户真实的聊天记录填充，禁止照抄模板中的示例占位文本。

## 安装说明（5 秒读完）

本技能是纯文本 SKILL.md，**不绑定任何平台**——Claude / GPT / Gemini / DeepSeek / OpenClaw 等任意 AI 通用，两种用法任选：

- **用法一：作为技能文件安装**（平台支持技能机制时，如 OpenClaw / Claude Code）
  1. 把整个 `shall-we-talk/` 文件夹放到该平台的技能目录，例如 `~/.openclaw/workspace/skills/shall-we-talk/`；
  2. 验证：`openclaw skills list` 能看到 `shall-we-talk` 即成功（其他平台按各自方式确认加载）。
- **用法二：直接粘贴使用**（任何 AI 通用）
  把本文件全文（含 frontmatter，模板内容可一并附上）粘贴给任意 AI，说一句"我们该不该摊牌"即可开始。

> 提示：传输 / 下载后文件名若带 `---` 暂存前缀（如 `SKILL---xxx.md`），先重命名为标准名再安装：`SKILL.md`、`README.md`、`templates/heart-report.md`、`templates/shall-we-talk-scripts.md`。

触发：对话里说"我们该不该摊牌"等触发词；支持 `/skill` 命令的平台也可用 `/skill shall-we-talk`。

---

遗憾的从来不是那句被拒绝的话，而是那句永远没有说出口的话。愿你在还来得及的时候，把想说的说出来——无论答案是什么，多年以后你记得的，是一个问过的人，而不是一个等过的人。

所有未言之心，皆是未竟之诗。
